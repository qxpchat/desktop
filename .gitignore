# Rust
server/target/
src-tauri/target/
target
.DS_Store

# Tauri-generated schemas — regenerated by tauri-build on every cargo
# build; the on-disk copies serve as IDE schema hints only.
src-tauri/gen/

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Local accounts dir (created by `cargo run`)
qxp-web-accounts/
references
TODOs*

# E2E test pool — real chatmail creds, regenerable via `make test-accounts`.
tests/pool.json
tests/node_modules/
tests/.pool-lock.json
# Leftover per-test accounts dirs land in /tmp by default but list the
# local fallback too in case a contributor symlinks one in.
tests/.tmp-accounts/
# Pre-paired account snapshots produced by `make test-accounts` — large
# binary SQLite dbs + blob dirs, regenerable.
tests/fixtures/account-templates/
# Attachment fixtures materialised at test start by `helpers/media.ts`
# (tiny base64-inlined files). Regenerable.
tests/fixtures/media/
tests/test-results
test-results
