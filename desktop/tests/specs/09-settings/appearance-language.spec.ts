// Phase 9 — Settings: Appearance / Language.
//
// TODO: language picker isn't in the Appearance pane yet. i18n is
// wired (lib/i18n/i18n.svelte) but exposed only via the inline `t()`
// calls; there's no user-facing locale switch. Skip until that
// surface lands.

import { test } from '../../fixtures/app-paired.js';

test.skip('Appearance: language picker not yet shipped', async () => {
  /* see header */
});
