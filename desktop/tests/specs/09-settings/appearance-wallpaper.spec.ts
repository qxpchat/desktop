// Phase 9 — Settings: Appearance / Wallpaper.
//
// TODO: qxp's Appearance pane currently exposes Theme + Accent + Text
// size; chat wallpaper isn't a setting in the UI yet. Skip until that
// surface lands.

import { test } from '../../fixtures/app-paired.js';

test.skip('Appearance: wallpaper picker not yet shipped', async () => {
  /* see header */
});
