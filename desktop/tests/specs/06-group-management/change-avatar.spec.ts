// Phase 6 — set / change a group avatar.
//
// TODO: ChatInfo's group header renders the avatar with no edit
// affordance — there's no tap-to-pick path or hidden file input
// wired to `set_chat_profile_image`. Spec deferred until that
// surface exists.

import { test } from '../../fixtures/app-paired.js';

test.skip('change-avatar: UI affordance not yet shipped', async () => {
  /* see header comment */
});
