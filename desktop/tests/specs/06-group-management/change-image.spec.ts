// Phase 6 — set / change a broadcast channel image.
//
// TODO: same status as change-avatar — ChatInfo for OutBroadcast chats
// renders the cover image but offers no upload affordance. Deferred.

import { test } from '../../fixtures/app-paired.js';

test.skip('change-image: UI affordance not yet shipped', async () => {
  /* see header comment */
});
