// Phase 13 — cross-cutting: jump from global search to bubble.
//
// Behaviorally identical to Phase 10's `message-search-global.spec.ts`
// (type into chat-list search → click "Messages" hit → ChatView opens
// with the matching bubble visible). Marking skip here so we don't
// double-bill the suite for the same exercise.

import { test } from '../../fixtures/app-paired.js';

test.skip('jump-from-search: covered by 10-search/message-search-global', async () => {
  /* see header */
});
