//! Placeholder for richer RPC wiring (per-connection event subscriptions, etc.)
//! that lands in later phases. For Phase 0, `CommandApi::from_arc` plus the
//! WebSocket pump in `ws.rs` is enough — no extra glue needed yet.
